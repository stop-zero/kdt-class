<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>폼 전송 결과 페이지</title>
    <style>
    </style>
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <li>이름: <%= userInfo.name %></li>
      <li>성별: <%= userInfo.gender %> </li>
      <li>생년월일:<%= userInfo.birth %></li>
      <li>취미: <%= userInfo.favorite %></li>
 <!--result 하나로 하고 if문 써서 index1 결과에서는 색상이랑 숫자가 안 보이게 -->
      <% if (userInfo.myColor) { %>
        <li>좋아하는 색상: <%= userInfo.myColor %></li>
        <% } %>
    
        <% if (userInfo.myNumber) { %>
        <li>좋아하는 숫자: <%= userInfo.myNumber %></li>
        <% } %>
    </ul>
    <a href="/">홈으로 이동하기</a>
  </body>
</html>
